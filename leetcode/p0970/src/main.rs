fn main() {
    println!("Hello, world!");
}
pub struct Solution{}

impl Solution {
    pub fn powerful_integers(x: i32, y: i32, bound: i32) -> Vec<i32> {
        let mut v = vec![];
        let mut temp = 0;
        while temp <= bound {
            temp = x.pow(
    }
}
